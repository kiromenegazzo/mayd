import styled from 'styled-components';

import { Typography } from '@features/ui/components/Typography';

export const Container = styled.div`
  display: flex;
  gap: 16px;
  align-items: center;
`;

export const Pages = styled(Typography)``;
